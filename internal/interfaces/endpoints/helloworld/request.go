package helloworld

import (
	"context"
	"net/http"
)

func DecodeHelloWorldRequest(ctx context.Context , r *http.Request) (request interface{}, err error) {
    return nil, nil
    // decode the request
}
